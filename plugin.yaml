name: byte-memory-core
version: 2.1.0
description: Local vector-indexed memory with BEAM tiers (Working/Episodic/Scratchpad) — FTS5 search, importance scoring, TF-IDF fallback, and auto-pruning.
author: "Byte"
provides_tools:
  - bmc_search
  - bmc_store
  - bmc_remember
  - bmc_forget
  - bmc_status
  - bmc_tier_move
  - bmc_reindex
